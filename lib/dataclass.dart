import 'package:flutter/material.dart';

class Dataclass {
  final String id;
  final String title;
  final Color color;
  final Icon icon;
  const Dataclass(
      {required this.id,
      required this.title,
      required this.color,
      required this.icon});
}
