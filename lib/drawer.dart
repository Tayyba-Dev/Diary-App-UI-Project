import 'package:flutter/material.dart';

class ADrawer extends StatelessWidget {
  const ADrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}
